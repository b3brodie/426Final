import { Group, TextGeometry, Object3D, FontLoader } from 'three';

class TitleText extends Group {
    constructor() {
        // Call parent Group() constructor
        super();

        this.name = 'titletext';

    }
}

export default TitleText;
